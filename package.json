{
  "name": "sns-watchtower",
  "version": "3.1.0",
  "description": "SNS Watchtower — Multi-server security & investigation Discord bot (part of Sentinel Network Systems)",
  "main": "index.js",
  "scripts": {
    "start":     "node index.js",
    "dev":       "node --watch index.js",
    "init":      "node scripts/initDB.js",
    "reset":     "node scripts/initDB.js --reset",
    "backup":    "node scripts/backupData.js",
    "validate":  "node scripts/validate.js"
  },
  "dependencies": {
    "discord.js": "^14.15.3",
    "dotenv": "^16.9.0"
  },
  "engines": { "node": ">=18.0.0" }
}
